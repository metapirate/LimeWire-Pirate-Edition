package org.limewire.core.api.connection;

/**
 * Used to differentiate if this node supports firewall transfers or not.
 */
public enum FirewallTransferStatus {
    SUPPORTS_FWT, DOES_NOT_SUPPORT_FWT
}
